<% ns = current_user.nodes.map do |n| %>
  <% ns = n.neighbours %>
  <% val = ns.reduce(ns.size) { |acc, e| acc += e.neighbours.size } %>
  <% {
    id: n.id,
    x: n.x,
    y: n.y,
    label: n.title,
    r: 10 + 2*val
  } %>
<% end %>
<%= raw ns.to_json %>
