%h1 Edit node #{@node.title}

= link_to('Delete node',                     |
          node_path(@node),                  |
          method: :delete,                   |
          class: 'btn btn-danger',           |
          data: { confirm: 'Are you sure?'}) |

= render 'form'
